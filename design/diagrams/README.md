# diagrams/

Diagrams are authored as Mermaid inline in the specifications (D1 and D3). Export rendered
versions here (SVG/PNG) for external sharing, or keep `.mmd` source files for standalone diagrams.

To render Mermaid to images, use the Mermaid CLI (`@mermaid-js/mermaid-cli`) or the tooling in
`tools/`.
